pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "ShoppingMemoKMMApplication"
include(":androidApp")
include(":shared")